{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <button style="margin: 10px;" class="btn btn-success btn-sm">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
